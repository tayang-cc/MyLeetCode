### 解题思路

此处撰写解题思路
交换左右指针的值，直到左右位置重合，返回。

### 代码

* java

```java
class Solution {
    public void reverseString(char[] s) {
        int left=0,right=s.length-1;
        while(left<=right){
            char ch=s[left];
            s[left]=s[right];
            s[right]=ch;
            left++;
            right--;

        }
        return ;

    }
}
```

